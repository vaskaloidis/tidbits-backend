const Stack = `
  type Stack {
    id: ID
    name: String
    description: String
    defaultLanguage: String
    tidbits: [Tidbit]
    createdAt: String
    updatedAt: String
  }
`;

export default Stack;
